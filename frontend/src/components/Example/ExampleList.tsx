import { Example } from "./Example";

import styles from "./Example.module.css";

export type ExampleModel = {
    text: string;
    value: string;
};

const EXAMPLES: ExampleModel[] = [
    {
        text: "What is the Policy about Outside Employment?",
        value: "What is the Policy about Outside Employment?"
    },
    { text: "What are the guidance around accepting gifts?", value: "What are the guidance around accepting gifts?" },
    { text: "Give me some examples of conflict of Interests", value: "Give me some examples of conflict of Interests" }
];

interface Props {
    onExampleClicked: (value: string) => void;
}

export const ExampleList = ({ onExampleClicked }: Props) => {
    return (
        <ul className={styles.examplesNavList}>
            {EXAMPLES.map((x, i) => (
                <li key={i}>
                    <Example text={x.text} value={x.value} onClick={onExampleClicked} />
                </li>
            ))}
        </ul>
    );
};
